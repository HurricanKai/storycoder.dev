name: Update Story List

on:
  push:
    branches: ["main"]
permissions:
  contents: read

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
      - name: Setup repo
        uses: actions/checkout@v3
        
      - name: Update List
        run: ./update-script.sh
